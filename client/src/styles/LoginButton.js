import styled from "styled-components";

const LoginButton = styled.a`
  textDecoration: none,
  color: #fff,
  padding: 15px 50px,
  background: #1DB954,
  borderRadius: 25px,
`;

export default LoginButton;
